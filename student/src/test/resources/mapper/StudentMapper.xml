<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.dao.StudentDao">
    <resultMap id="studentMap" type="com.example.entity.Student">
        <result column="id" property="id"/>
        <result column="student_name" property="studentName"/>
        <result column="age" property="age"/>
        <result column="sex" property="sex"/>
        <result column="birthday" property="birthday"/>
    </resultMap>
    <insert id="save" parameterType="com.example.entity.Student">
        INSERT INTO
          student
          (student_name, age, sex, birthday)
        VALUE
        (#{studentName}, #{age}, #{sex}, #{birthday})
    </insert>
    <delete id="remove" parameterType="java.lang.Long">
        DELETE FROM
          student
        WHERE
          id = #{id}
    </delete>
    <update id="update" parameterType="com.example.entity.Student">
        UPDATE
          student
        SET
          student_name = #{studentName}, age = #{age}, sex = #{sex}, birthday = #{birthday}
        WHERE
          id = #{id}
    </update>
    <select id="getStudentById" parameterType="java.lang.Long">
        SELECT
          *
        FROM
          student
        WHERE
          id = #{id}
    </select>
</mapper>