<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.dao.UserDao">
    <resultMap id="userMap" type="com.example.entity.User">
        <result column="id" property="userId"/>
        <result column="username" property="username"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
        <result column="password" property="password"/>
        <result column="last_login_time" property="lastLoginTime"/>
        <result column="role" property="role"/>
    </resultMap>
    <insert id="save" parameterType="com.example.entity.User">
        INSERT INTO
          user
          (username,email,phone,password,last_login_time,roles)
        VALUES
        (#{username},#{email},#{phone},#{password},#{lastLoginTime},#{roles})

    </insert>
    <delete id="remove" parameterType="java.lang.Long">
        DELETE FROM
            user
          WHERE
            id = #{id}
    </delete>
    <update id="updateUser" parameterType="com.example.entity.User">
        UPDATE
          user
          SET
            username = #{username},password = #{password}
          WHERE
            id = #{id}
    </update>
    <select id="getUserById" parameterType="java.lang.Long" resultMap="userMap">
        SELECT
          *
          FROM
          user
          WHERE
           id = #{id}
    </select>
    <select id="getUserByEmail" parameterType="java.lang.String" resultMap="userMap">
        SELECT 
          *
          FROM 
          user
          WHERE email = #{email}
    </select>
    <select id="getUserByPhone" parameterType="java.lang.String" resultMap="userMap">
        SELECT 
          *
          FROM 
          users
          WHERE phone = #{phone}
    </select>
    <select id="listUserAll" resultMap="userMap">
        SELECT * FROM user
    </select>
</mapper>